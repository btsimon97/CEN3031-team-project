{
  "name": "my-app",
  "version": "1.0.0",
  "description": "Medical instrument",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install && node server.js",
    "client": "cd client && npm install --prefix client npm run build --prefix client",
    "heroku-postbuild": "npm run client",
    "dev": "concurrently \"nodemon server.js\" \"cd client && npm start\""
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "i": "^0.3.6",
    "mongoose": "^5.9.7",
    "morgan": "^1.10.0",
    "multer": "^1.4.2",
    "react": "^16.13.1",
    "react-scripts": "^3.4.1"
  },
  "type": "module",
  "homepage": "https://instrument-app.herokuapp.com/",
  "engines": {
    "node": "13.x"
  }
}
