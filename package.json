{
  "name": "my-app",
  "version": "1.0.0",
  "description": "Medical instrument",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install && node server.js",
    "build":  "cd client && npm run build",
    "client": "cd client && npm install npm run build",
    "heroku-postbuild": "npm run client && npm start"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "i": "^0.3.6",
    "mongoose": "^5.9.7",
    "morgan": "^1.10.0",
    "multer": "^1.4.2",
    "react": "^16.13.1",
    "react-scripts": "^3.4.1"
  },
  "type": "module",
  "homepage": "https://instrument-app.herokuapp.com/",
  "engines": {
    "node": "13.x"
  }
}
